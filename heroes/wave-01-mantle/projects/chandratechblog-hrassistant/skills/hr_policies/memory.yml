schema:
  public:
    topic:
      type: categorical
      enum_values: [attendance, leave, remote_work, conduct, benefits, payroll, general]
      description: The HR policy topic selected by the employee.
      llm_settable: true
