# Release line that `make latest` / `--latest` must stay within.
#
# Every resource in this catalog imports the Mantle engine. Rasa renamed that
# package from `rasa.calm_v2` to `rasa.mantle` in 3.20.0.dev1, and the old path
# is gone rather than aliased — so the import name and the pin move together.
#
# Verified against PyPI on 2026-08-26 by inspecting the published wheels:
#   3.20.0.dev1  ships rasa/mantle/       <- current line
#   3.19.0.dev7  ships rasa/calm_v2/      <- previous line
#   3.19.1       ships NEITHER            <- stable still has no engine
#
# The line exists because the newest *stable* release still does not carry the
# engine at all. Delete this file once a stable release ships `rasa.mantle`;
# `make outdated` will tell you when that happens.
3.20.0.dev
