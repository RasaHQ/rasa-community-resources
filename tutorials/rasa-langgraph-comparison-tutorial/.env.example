# Copy to .env and fill in. Never commit .env.
#
# Both values are required. The travel database is built locally from the
# committed fixture — there is nothing to download.
RASA_LICENSE=
OPENAI_API_KEY=
