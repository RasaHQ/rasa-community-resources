# The tutorial is text-first on purpose: the teaching points are tool scope and
# memory scope, so it needs one LLM key and nothing else.

llm:
  provider: openai
  model: gpt-4.1-mini
  api_key_env: OPENAI_API_KEY
  temperature: 0.0

channels:
  rest:
    enabled: true
  inspector:
    enabled: true
