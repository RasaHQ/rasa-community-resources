# Voice in and out through Deepgram. Swap the provider blocks to change vendor;
# nothing else in this project depends on which one is used.
asr:
  deepgram:
    api_key: ${DEEPGRAM_API_KEY}
tts:
  deepgram:
    api_key: ${DEEPGRAM_API_KEY}
