[project]
name = "voice-handoff-context"
version = "0.1.0"
description = "Pattern: hand a caller to a human as a state transfer — structured context package, derived summary, and an allowlist governing what crosses"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "rasa-pro==3.20.0.dev6",
]

[tool.uv]
prerelease = "allow"
package = false
