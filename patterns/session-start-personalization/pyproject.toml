[project]
name = "session-start-personalization"
version = "0.1.0"
description = "Pattern: personalize every conversation from a session-start profile lookup"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "rasa-pro==3.20.0.dev6",
]

[tool.uv]
prerelease = "allow"
package = false
