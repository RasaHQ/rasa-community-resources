[project]
name = "voice-auth-stepup"
version = "0.1.0"
description = "Pattern: risk-tiered authentication step-up, resolved when an action is attempted"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "rasa-pro==3.20.0.dev6",
]

[tool.uv]
prerelease = "allow"
package = false
