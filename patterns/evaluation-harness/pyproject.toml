[project]
name = "evaluation-harness"
version = "0.1.0"
description = "Pattern: measure a Mantle agent with assertion-based e2e tests, dialogue-understanding simulation, and LLM-as-a-judge scoring"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "rasa-pro==3.20.0.dev6",
]

[tool.uv]
prerelease = "allow"
package = false
