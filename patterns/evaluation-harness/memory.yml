# Project-wide memory shared across every skill (resolves to session.project.*).
# The fixture agent keeps exactly one project-level field: enough to prove that
# `slot_was_set` assertions can observe cross-skill state, not enough to make the
# agent the subject of this pattern.
customer_name:
  type: text
  description: Name to address the customer by. Seeded at session start.
