# Paste-ready snippets

Each `step-NN-*` folder contains complete files for that tutorial chapter.
Copy them into the matching project paths (usually `skills/<name>/` or project root).

Prefer the finished files in the repo root when the live session falls behind.
