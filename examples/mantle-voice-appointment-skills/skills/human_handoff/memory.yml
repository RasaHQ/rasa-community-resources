schema:
  public:
    handoff_reason:
      type: text
      description: Why the patient wants to speak to a person.
      llm_settable: true
    handoff_confirmed:
      type: bool
      description: Patient confirmed they want a callback from the clinic team.
      llm_settable: true
    handoff_created:
      type: bool
    handoff_ticket_id:
      type: text
