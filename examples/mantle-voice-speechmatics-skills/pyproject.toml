[project]
name = "vela-voice-speechmatics"
version = "0.1.0"
description = "Voice banking agent on Rasa Mantle with a custom Speechmatics ASR engine"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "rasa-pro==3.20.0.dev1",
]

[tool.uv]
prerelease = "allow"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lib", "engines"]
