schema:
  public:
    account_type:
      type: text
      description: Account the caller named, in their own words (checking or savings).
      llm_settable: true
    # Tool-set only, so it carries no description — the engine rejects
    # descriptions on fields the LLM cannot set.
    account_balance:
      type: float
